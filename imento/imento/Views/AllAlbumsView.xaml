<Page
    x:Class="imento.AllAlbumsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:imento"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:data="using:imento.Models"
    mc:Ignorable="d">

    <Grid Padding="60,0,0,0"  Background="White">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <ListView Grid.Row="1" Grid.Column="1" ItemsSource="{x:Bind Albums}" IsItemClickEnabled="True" ItemClick="GridView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="data:Album">
                    <StackPanel Orientation="Vertical" Height="100" Width="800"  Background="{StaticResource LightOrange}" Padding="5,5,5,5" Margin="0,15,30,15">
                        <!-- <Image Width="200" Source="{x:Bind PicSource}"/> -->
                        <TextBlock FontSize="38" Text="{x:Bind Title}" TextWrapping="Wrap" />
                        <TextBlock FontSize="18" Text="{x:Bind Description}" TextWrapping="Wrap" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>


    </Grid>

</Page>
